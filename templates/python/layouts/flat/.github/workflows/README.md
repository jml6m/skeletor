# GitHub Workflows

Basic CI runs the same checks as this template's `verifyCommands` in `template.json` (ruff, pytest, mypy).

Customize as needed (matrix Python versions, coverage, deployment, etc.).