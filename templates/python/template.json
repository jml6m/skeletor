{
  "id": "python",
  "name": "Python",
  "description": "Modern Python with best-practice defaults: pyproject.toml, ruff (lint + format), pytest, basic typing layout, src/ structure, and an AGENTS.md contract.",
  "language": "python",
  "verifyCommands": [
    "python -m pip install -e '.[dev]' --quiet",
    "python -m ruff format --check .",
    "python -m ruff check .",
    "python -m pytest -q"
  ]
}