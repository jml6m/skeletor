# Port registry

| Project | Port | Owner script |
|---------|------|--------------|
| {{PROJECT_NAME}} | {{APP_PORT}} | `npm run free-port` (runs before `start`) |

Claim the next free port in your workspace range when adding another local server.
Record it here so teammates avoid collisions.